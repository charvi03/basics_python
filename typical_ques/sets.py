# Python code to get the maximum element from a set
def MAX(sets):
    return (max(sets))


def MIN(sets):
    return (min(sets))


# Driver Code
sets = set((4, 12, 10, 9, 4, 13))
print(MAX(sets))
print(MIN(sets))
